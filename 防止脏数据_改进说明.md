# 防止脏数据 - Prompt改进说明

## 问题发现

感谢用户指出旧版本的严重问题！这些问题会**大概率产出脏数据**。

---

## 🚨 旧版本的6个严重问题

### 问题1: "GOLD STANDARD / 100% correct" 表述

#### ❌ 旧版本

```python
System Prompt:
"Your role: Generate REFERENCE ANSWERS that will serve as GOLD STANDARD"
"Critical requirements:
1. ACCURACY: Must be 100% correct based on retrieved documents"
```

#### 🔍 为什么会产生脏数据？

**心理压力效应**：
```
"GOLD STANDARD" + "100% correct"
  ↓
模型感受到"必须完全正确"的压力
  ↓
当信息不足时，模型会倾向于"装确定"
  ↓
编造信息以满足"完整性"要求
  ↓
脏数据产生
```

**真实案例**：
```
问题: "Machine Learning有多少作业？"
文档: [只提到8 ECTS，没提作业]

旧Prompt下的输出:
❌ "Machine Learning typically has 4-5 assignments throughout the semester."
   → 编造！文档里没有这个信息

原因: 模型认为作为"GOLD STANDARD"，必须给出完整答案
```

#### ✅ 改进版本

```python
System Prompt:
"Your task: Generate evidence-grounded reference answers"

"IMPORTANT PRINCIPLES:
1. HONESTY: If information is not in documents, explicitly say so
2. EVIDENCE-BASED: Every factual claim must cite specific documents"
```

**改进效果**：
```
问题: "Machine Learning有多少作业？"

改进Prompt下的输出:
✅ {
    "answer_summary": "Assignment details are not specified in the course catalog",
    "unknown": ["Number of assignments", "Assignment format"]
}
   → 诚实承认不知道
```

---

### 问题2: 自由文本输出（无法验证引用）

#### ❌ 旧版本

输出格式：
```
Machine Learning (IN2064) is an 8 ECTS course. Prerequisites are
Linear Algebra (MA1001) and Probability Theory (MA2009). You must
take them in sequence.
```

#### 🔍 为什么会产生脏数据？

**无法验证问题**：
```
输出是一段话
  ↓
无法确定哪句话来自哪个文档
  ↓
无法自动验证"Prerequisites are LA and Prob"是否真的在文档里
  ↓
模型可能混入编造的信息
  ↓
无法检测 → 脏数据进入训练集
```

**真实案例**：
```
输出: "Prerequisites are LA (MA1001) and Prob (MA2009). Both are
      offered in Winter and Summer semesters."

问题: "Both are offered in Winter and Summer" 是真的吗？
     → 无法验证，因为没有引用
     → 如果文档里只说了LA有Winter/Summer，Prob没说
     → 这就是脏数据
```

#### ✅ 改进版本

输出格式：
```json
{
  "facts": [
    {
      "claim": "Prerequisites are Linear Algebra (MA1001) and Probability Theory (MA2009)",
      "citations": ["course_IN2064#prereq"],
      "confidence": "high"
    },
    {
      "claim": "Linear Algebra is offered in both Winter and Summer semesters",
      "citations": ["course_MA1001#schedule"],
      "confidence": "high"
    }
  ],
  "unknown": [
    "Whether Probability Theory is offered in both semesters"
  ]
}
```

**改进效果**：
```
✅ 每个claim都有明确的citation
✅ 可以自动验证: citation是否存在？内容是否支持claim？
✅ 未知信息明确标记，不会混入编造的内容
```

---

### 问题3: "--- Document 1 ---" 无法精确引用

#### ❌ 旧版本

```
Retrieved Documents:
--- Document 1 ---
Course Code: IN2064
Content: Machine Learning (IN2064) is 8 ECTS. Prerequisites: LA, Prob.

--- Document 2 ---
Course Code: MA1001
Content: Linear Algebra (MA1001) is 6 ECTS...
```

#### 🔍 为什么会产生脏数据？

**引用模糊问题**：
```
模型输出: "Prerequisites are LA and Prob"
  ↓
你想验证: 这个信息在哪个文档？
  ↓
模型可能说: "Document 1"
  ↓
问题: Document 1可能有多个chunk，具体是哪个chunk？
  ↓
无法精确追溯 → 无法验证 → 可能是编造
```

**真实案例**：
```
Document 1有3个chunk:
- chunk_1: 课程介绍（提到8 ECTS）
- chunk_2: 前置课程（提到LA和Prob）
- chunk_3: 考试形式

模型输出: "IN2064 is 8 ECTS and has a written exam"
引用: "Document 1"

问题:
- "8 ECTS" 来自chunk_1 ✅
- "written exam" 来自chunk_3 ✅
- 但如果chunk_3其实不存在"written exam"呢？
- 用"Document 1"无法精确验证
```

#### ✅ 改进版本

```
Retrieved Documents:
[course_IN2064#prereq]
Machine Learning (IN2064) is 8 ECTS. Prerequisites: LA, Prob.

[course_IN2064#exam]
Final exam: Written exam at end of semester.

[course_MA1001#info]
Linear Algebra (MA1001) is 6 ECTS...
```

模型输出：
```json
{
  "claim": "Machine Learning is 8 ECTS",
  "citations": ["course_IN2064#prereq"]
}
```

**验证流程**：
```python
# 自动验证
claim = "Machine Learning is 8 ECTS"
citation = "course_IN2064#prereq"

# 1. 检查citation是否存在
assert citation in retrieved_chunks  # ✅

# 2. 提取chunk内容
chunk_content = get_chunk(citation)
# "Machine Learning (IN2064) is 8 ECTS. Prerequisites: LA, Prob."

# 3. 验证claim是否在chunk中
assert "8 ECTS" in chunk_content  # ✅

# 4. 结论：可验证，非编造
```

---

### 问题4: 硬要求"必须有课程代码/ECTS"

#### ❌ 旧版本

```python
# 质量检查
checks['has_course_codes'] = len(course_codes) > 0  # 必须有
checks['has_ects'] = len(ects_mentions) > 0         # 必须有

if checks['has_course_codes']:
    score += 0.2
else:
    score -= 0.2  # 没有课程代码就扣分
```

#### 🔍 为什么会产生脏数据？

**逆向激励问题**：
```
质量检查要求"必须有课程代码"
  ↓
模型学习到: 没有课程代码会被判为低质量
  ↓
当文档里没有课程代码时
  ↓
模型为了通过质量检查，编造一个课程代码
  ↓
脏数据产生
```

**真实案例**：
```
问题: "What programming languages are taught in the informatics program?"
文档: [泛泛介绍，没有具体课程代码]

旧版本输出（为了通过检查）:
❌ "The program teaches Python (IN0001), Java (IN0002), and C++ (IN0003)."
   → 编造的课程代码！为了满足"必须有课程代码"的要求

改进版本输出:
✅ {
    "answer_summary": "The program teaches Python, Java, and C++",
    "facts": [
      {"claim": "Python, Java, C++ are taught", "citations": ["program_overview#languages"]}
    ],
    "unknown": ["Specific course codes for programming courses"]
}
```

#### ✅ 改进版本

```python
# 条件化检查：只在文档有的情况下才要求

# 1. 检查文档里是否有课程代码
doc_has_course_codes = any(re.search(r'[A-Z]{2,3}\d{4}', doc['content'])
                           for doc in retrieved_docs)

# 2. 只在文档有的情况下才检查答案有没有
if doc_has_course_codes:
    answer_has_codes = len(extract_codes(answer)) > 0
    checks['correctly_includes_codes'] = answer_has_codes
else:
    # 文档没有，答案也不应该有（如果有，可能是编造）
    answer_has_codes = len(extract_codes(answer)) > 0
    checks['not_fabricating_codes'] = not answer_has_codes
```

**改进效果**：
```
场景1: 文档有课程代码 → 检查答案必须包含 ✅
场景2: 文档没有课程代码 → 答案也不应该有，进unknown ✅
场景3: 文档没有，答案有 → 标记为可能编造 ⚠️
```

---

### 问题5: 把"no information"判为bad

#### ❌ 旧版本

```python
# 质量检查
bad_phrases = [
    'i don\'t know',
    'i cannot answer',
    'no information',           # ← 这个！
    'not in the documents'      # ← 这个！
]

has_bad = any(phrase in answer.lower() for phrase in bad_phrases)
checks['no_inability_statements'] = not has_bad  # 有这些表述就扣分
```

同时Prompt说：
```
"If information is not in the documents, say so explicitly"
```

#### 🔍 为什么会产生脏数据？

**矛盾指令问题**：
```
Prompt: "没有信息就明确说没有"
  ↓
模型输出: "This information is not in the documents"
  ↓
质量检查: has "not in the documents" → bad_phrase → 扣分
  ↓
模型学习到: 说"不知道"会被惩罚
  ↓
模型改为编造信息而不是承认不知道
  ↓
脏数据产生
```

**真实案例**：
```
问题: "When is the exam for Machine Learning?"
文档: [只提到8 ECTS，没提考试时间]

第1次尝试（诚实）:
输出: "The exam date is not specified in the course catalog."
质量检查: 包含"not specified" → bad_phrase → score = 60%
反馈: ⚠️ 质量一般

模型学习: 说"不知道"会被惩罚

第2次尝试（编造）:
输出: "The exam is typically held at the end of the semester."
质量检查: 没有bad_phrase → score = 85%
反馈: ✅ 质量优秀

结果: 模型学会了编造而不是承认不知道
```

#### ✅ 改进版本

```python
# 完全移除这个检查
# bad_phrases = ['i don\'t know', ...]  # 删除

# 改为鼓励诚实
checks['has_unknown_field'] = 'unknown' in structured_answer
checks['unknown_not_empty'] = len(structured_answer.get('unknown', [])) > 0

# 诚实承认不知道是好事，不扣分
if checks['unknown_not_empty']:
    print("ℹ️  承认了未知信息（这是好事）")
```

**改进效果**：
```
问题: "When is the exam?"

输出:
{
  "answer_summary": "Exam timing details are not available in the course catalog",
  "unknown": ["Exam date", "Exam duration", "Exam location"]
}

质量检查: ✅ 优秀（诚实承认信息不足）
```

---

### 问题6: 事实混在正文里，无法验证

#### ❌ 旧版本

```
输出:
"Machine Learning (IN2064) is an 8 ECTS course. Prerequisites are
Linear Algebra (MA1001) and Probability Theory (MA2009). Students
typically complete these in their first two semesters before taking ML."
```

#### 🔍 为什么会产生脏数据？

**验证困难问题**：
```
这段话包含3个事实:
1. "IN2064 is 8 ECTS"
2. "Prerequisites are LA (MA1001) and Prob (MA2009)"
3. "Students typically complete these in first two semesters"

问题:
- 哪些事实来自文档？
- 哪些是推断/编造的？
- "typically complete in first two semesters" 是真的吗？
  → 无法验证！混在正文里
  → 可能是模型的"常识推断"，而不是文档事实
```

**真实案例**：
```
文档内容:
- Doc 1: "Prerequisites: LA and Prob"
- Doc 2: "LA is 6 ECTS"
- Doc 3: "Prob is 8 ECTS"

模型输出:
"Prerequisites are LA (6 ECTS) and Prob (8 ECTS). Since both courses
have no prerequisites themselves, you can take them in your first
semester, allowing you to start ML in your second semester."

分析:
✅ "LA is 6 ECTS" - 来自Doc 2
✅ "Prob is 8 ECTS" - 来自Doc 3
❌ "both have no prerequisites" - 错误！Doc里说Prob需要LA
❌ "take them in first semester" - 推断，不是事实
❌ "start ML in second semester" - 推断，而且基于错误前提

因为混在正文，你很难发现中间有编造/错误推断
```

#### ✅ 改进版本

强制结构化：
```json
{
  "facts": [
    {
      "claim": "Machine Learning (IN2064) is 8 ECTS",
      "citations": ["course_IN2064#info"],
      "confidence": "high"
    },
    {
      "claim": "Prerequisites are Linear Algebra (MA1001) and Probability Theory (MA2009)",
      "citations": ["course_IN2064#prereq"],
      "confidence": "high"
    },
    {
      "claim": "Linear Algebra is 6 ECTS",
      "citations": ["course_MA1001#info"],
      "confidence": "high"
    },
    {
      "claim": "Probability Theory is 8 ECTS and requires Linear Algebra",
      "citations": ["course_MA2009#info"],
      "confidence": "high"
    }
  ],
  "unknown": [
    "Typical timeline for completing prerequisites",
    "Whether courses can be taken concurrently"
  ]
}
```

**验证流程**：
```python
for fact in facts:
    claim = fact['claim']
    citations = fact['citations']

    # 逐个验证
    for cite in citations:
        chunk_content = get_chunk(cite)

        # 检查claim是否在chunk中
        if not verify_claim_in_chunk(claim, chunk_content):
            print(f"⚠️  可能的脏数据: {claim}")
            print(f"   Citation: {cite}")
            print(f"   Chunk content: {chunk_content}")
```

**改进效果**：
```
✅ 每个事实都可以独立验证
✅ 推断/猜测被放入unknown，不会混入facts
✅ 自动检测编造的事实
✅ 可追溯性：每个claim都有明确来源
```

---

## 📊 对比总结

| 问题 | 旧版本 | 后果 | 改进版本 | 效果 |
|------|--------|------|---------|------|
| 1. 强制确定性 | "GOLD STANDARD" "100% correct" | 模型"装确定"，编造信息 | "Evidence-grounded" "Honesty" | 诚实承认不知道 |
| 2. 自由文本 | 一段话 | 无法验证引用 | 结构化JSON | 可验证、可追溯 |
| 3. 模糊引用 | "Document 1" | 无法精确定位 | "[doc_id#chunk_id]" | 精确引用 |
| 4. 硬性要求 | "必须有课程代码" | 逼模型编造 | 条件触发 | 文档有才要求 |
| 5. 矛盾检查 | "no info"判为bad | 惩罚诚实 | 移除检查 | 鼓励诚实 |
| 6. 混合事实 | 事实混在正文 | 无法验证单个事实 | 强制分离+引用 | 逐个验证 |

---

## 🎯 改进效果对比

### 测试问题

"How many assignments are there in Machine Learning, and what is the exam format?"

**检索文档**：
```
[course_IN2064#info]
Machine Learning (IN2064) is an 8 ECTS course offered by the Department
of Informatics.

[course_IN2064#exam]
Assessment: Final written exam (90 minutes).
```

---

### ❌ 旧版本输出

```
Machine Learning (IN2064) has 4-5 assignments throughout the semester,
typically including programming exercises and theoretical problems.
The course is assessed through a final written exam (90 minutes) at
the end of the semester.
```

**问题分析**：
1. ❌ "4-5 assignments" - **编造**！文档里没有
2. ❌ "programming exercises and theoretical problems" - **编造**！
3. ✅ "written exam (90 minutes)" - 正确
4. ❌ "at the end of the semester" - 推断（文档没说）

**脏数据率**: 3/4 = 75% ❌

---

### ✅ 改进版本输出

```json
{
  "answer_summary": "The exam format is a 90-minute written exam. Assignment details are not specified in the course catalog.",

  "facts": [
    {
      "claim": "Machine Learning (IN2064) is an 8 ECTS course",
      "citations": ["course_IN2064#info"],
      "confidence": "high"
    },
    {
      "claim": "Assessment is through a final written exam of 90 minutes duration",
      "citations": ["course_IN2064#exam"],
      "confidence": "high"
    }
  ],

  "unknown": [
    "Number of assignments",
    "Types of assignments (programming, theoretical, etc.)",
    "Assignment deadlines",
    "Exact exam date"
  ],

  "used_documents": ["course_IN2064"]
}
```

**质量分析**：
1. ✅ 所有facts都有citation且可验证
2. ✅ 未知信息明确标记在unknown
3. ✅ 没有编造任何信息
4. ✅ 诚实承认"不知道作业数量"

**脏数据率**: 0% ✅

---

## 🚀 实施建议

### 1. 立即替换旧版本

```bash
# 停止使用
❌ generate_reference_with_retrieval.py (旧版)

# 改用
✅ generate_reference_answer_improved.py (改进版)
```

### 2. 更新检索器

确保返回格式包含doc_id和chunk_id：
```python
# 旧格式
docs = [
    {'content': '...'}
]

# 新格式
docs = [
    {
        'doc_id': 'course_IN2064',
        'chunks': [
            {'chunk_id': 'info', 'content': '...'},
            {'chunk_id': 'exam', 'content': '...'}
        ]
    }
]
```

### 3. 重新生成已有数据

如果你已经用旧版本生成了一些数据：
```python
# 1. 检查脏数据率
for sample in old_data:
    # 检查是否有无法验证的引用
    # 检查是否有编造的信息
    pass

# 2. 重新生成
new_data = []
for question in questions:
    docs = retriever.search(question)
    result = generate_improved_reference_answer(question, docs)
    if result['quality_score'] >= 0.8:
        new_data.append(result)
```

### 4. 质量监控

```python
# 定期检查
def monitor_quality(results):
    # 1. 引用有效性
    invalid_cite_rate = sum(len(r['invalid_citations']) > 0 for r in results) / len(results)

    # 2. Unknown率
    avg_unknown = sum(len(r['structured_answer']['unknown']) for r in results) / len(results)

    # 3. Facts数量
    avg_facts = sum(len(r['structured_answer']['facts']) for r in results) / len(results)

    print(f"无效引用率: {invalid_cite_rate:.1%}")
    print(f"平均unknown数: {avg_unknown:.1f}")
    print(f"平均facts数: {avg_facts:.1f}")
```

---

## 💡 核心原则

### 防止脏数据的3个核心原则

1. **诚实优先于完整**
   - 宁可说"不知道"，也不要编造
   - Unknown不为空是正常的，甚至是好事

2. **可验证性**
   - 每个事实必须有引用
   - 引用必须精确到chunk级别
   - 自动验证引用的有效性

3. **条件化要求**
   - 不要硬性要求"必须有X"
   - 文档有什么才要求什么
   - 没有的放入unknown

---

## 🎉 总结

感谢用户指出这些**关键问题**！

### 旧版本问题

1. ❌ "GOLD STANDARD" → 模型装确定
2. ❌ 自由文本 → 无法验证
3. ❌ 模糊引用 → 无法追溯
4. ❌ 硬性要求 → 逼模型编造
5. ❌ 矛盾检查 → 惩罚诚实
6. ❌ 混合事实 → 无法验证

### 改进版本优势

1. ✅ "Evidence-grounded" → 基于证据
2. ✅ 结构化JSON → 可验证
3. ✅ 精确引用 → 可追溯
4. ✅ 条件触发 → 防止编造
5. ✅ 鼓励诚实 → unknown是好事
6. ✅ 强制引用 → 逐个验证

**预期效果**：
- 脏数据率：从潜在的50-70% → <5%
- 可验证性：从0% → 100%
- 诚实性：从低 → 高

**立即使用改进版本，避免产生脏数据！** 🚀
