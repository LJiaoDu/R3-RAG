# 针对你的TUM数据的实际操作指南

## 📦 你的数据清单

根据你的描述，你有：

1. **200门课程介绍** - 简短信息
2. **8个模块的规划** - 学分要求、课程列表
3. **30门课程的详细材料**：
   - 课程讲义PDF
   - 练习题
   - Script（课程脚本/笔记）
   - 参考文献
4. **2个项目的要求文档** - 学业要求、专业要求

---

## 📁 第1步：组织你的文件

创建清晰的文件夹结构：

```bash
mkdir -p /home/user/tum_data
cd /home/user/tum_data

# 创建子文件夹
mkdir -p 1_course_intros        # 200门课程介绍
mkdir -p 2_module_plans         # 8个模块规划
mkdir -p 3_lectures             # 30门课程讲义
mkdir -p 4_exercises            # 30门练习题
mkdir -p 5_scripts              # 30门Script
mkdir -p 6_references           # 30门参考文献
mkdir -p 7_project_docs         # 2个项目文档
```

### 把文件放到对应文件夹：

```
/home/user/tum_data/
├── 1_course_intros/
│   ├── IN2064_intro.txt        (或.pdf)
│   ├── IN2118_intro.txt
│   └── ... (200个文件)
│
├── 2_module_plans/
│   ├── module1_kernmodule.pdf
│   ├── module2_vertiefung.pdf
│   └── ... (8个文件)
│
├── 3_lectures/
│   ├── IN2064_lecture_full.pdf
│   ├── IN2118_lecture.pdf
│   └── ... (30个文件)
│
├── 4_exercises/
│   ├── IN2064_exercise01.pdf
│   ├── IN2064_exercise02.pdf
│   └── ... (30门课的练习)
│
├── 5_scripts/
│   ├── IN2064_script.pdf
│   └── ...
│
├── 6_references/
│   ├── IN2064_references.txt
│   └── ...
│
└── 7_project_docs/
    ├── bachelor_thesis_requirements.pdf
    └── master_thesis_requirements.pdf
```

---

## 🔧 第2步：提取文本（阶段1）

使用之前的提取脚本，但**分别处理每个文件夹**：

```bash
cd /home/user/R3-RAG/data/scripts

# 提取课程介绍
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/1_course_intros \
    --output extracted_course_intros.json

# 提取模块规划
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/2_module_plans \
    --output extracted_module_plans.json

# 提取讲义
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/3_lectures \
    --output extracted_lectures.json

# 提取练习题
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/4_exercises \
    --output extracted_exercises.json

# 提取Script
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/5_scripts \
    --output extracted_scripts.json

# 提取参考文献
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/6_references \
    --output extracted_references.json

# 提取项目文档
python3 fixed_step1_extract.py \
    --input /home/user/tum_data/7_project_docs \
    --output extracted_projects.json
```

---

## 🧹 第3步：清洗所有文本（阶段2）

```bash
# 清洗所有提取的文本
for file in extracted_*.json; do
    output="cleaned_${file#extracted_}"
    python3 fixed_step2_clean.py --input "$file" --output "$output"
done
```

---

## ✂️ **第4步：智能切分（关键！）**

这是关键步骤！不同类型的文档用不同的切分策略。

创建切分脚本：`process_all_types.py`

```python
import json
from smart_chunker_for_tum import TUMDocumentChunker

chunker = TUMDocumentChunker()

# ==========================================
# 1. 课程介绍（200门）- 不切分
# ==========================================
print("处理课程介绍...")
with open('cleaned_course_intros.json', 'r', encoding='utf-8') as f:
    course_intros = json.load(f)

for doc in course_intros:
    # 从文件名提取课程代码（如 IN2064_intro.pdf → IN2064）
    filename = doc['source_file']
    course_code = filename.split('_')[0] if '_' in filename else filename.replace('.pdf', '')

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='course_intro',
        doc_id=course_code
    )

print(f"✅ 课程介绍处理完成")

# ==========================================
# 2. 模块规划（8个）- 按模块切分
# ==========================================
print("\n处理模块规划...")
with open('cleaned_module_plans.json', 'r', encoding='utf-8') as f:
    module_plans = json.load(f)

for doc in module_plans:
    module_name = doc['source_file'].replace('.pdf', '')

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='module_plan',
        doc_id=module_name
    )

print(f"✅ 模块规划处理完成")

# ==========================================
# 3. 课程讲义（30门）- 标准切分（1500字符）
# ==========================================
print("\n处理课程讲义...")
with open('cleaned_lectures.json', 'r', encoding='utf-8') as f:
    lectures = json.load(f)

for doc in lectures:
    course_code = doc['source_file'].split('_')[0]

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='lecture',
        doc_id=course_code
    )

print(f"✅ 课程讲义处理完成")

# ==========================================
# 4. 练习题（30门）- 按题切分
# ==========================================
print("\n处理练习题...")
with open('cleaned_exercises.json', 'r', encoding='utf-8') as f:
    exercises = json.load(f)

for doc in exercises:
    filename = doc['source_file']
    # 如 IN2064_exercise01.pdf
    course_code = filename.split('_')[0]
    exercise_id = filename.replace('.pdf', '')

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='exercise',
        doc_id=exercise_id
    )

print(f"✅ 练习题处理完成")

# ==========================================
# 5. Script（30门）- 标准切分
# ==========================================
print("\n处理Script...")
with open('cleaned_scripts.json', 'r', encoding='utf-8') as f:
    scripts = json.load(f)

for doc in scripts:
    course_code = doc['source_file'].split('_')[0]

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='script',
        doc_id=course_code
    )

print(f"✅ Script处理完成")

# ==========================================
# 6. 参考文献（30门）- 不切分或按条目切
# ==========================================
print("\n处理参考文献...")
with open('cleaned_references.json', 'r', encoding='utf-8') as f:
    references = json.load(f)

for doc in references:
    course_code = doc['source_file'].split('_')[0]

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='references',
        doc_id=course_code
    )

print(f"✅ 参考文献处理完成")

# ==========================================
# 7. 项目文档（2个）- 按章节切分
# ==========================================
print("\n处理项目文档...")
with open('cleaned_projects.json', 'r', encoding='utf-8') as f:
    projects = json.load(f)

for doc in projects:
    project_name = doc['source_file'].replace('.pdf', '')

    chunker.process_document(
        text=doc['cleaned_text'],
        doc_type='project',
        doc_id=project_name
    )

print(f"✅ 项目文档处理完成")

# ==========================================
# 导出
# ==========================================
chunker.export_to_jsonl('final_corpus.jsonl')
chunker.print_statistics()
```

运行：
```bash
python3 process_all_types.py
```

**输出示例**：
```
处理课程介绍...
✅ 课程介绍处理完成

处理模块规划...
✅ 模块规划处理完成

处理课程讲义...
✅ 课程讲义处理完成

处理练习题...
✅ 练习题处理完成

处理Script...
✅ Script处理完成

处理参考文献...
✅ 参考文献处理完成

处理项目文档...
✅ 项目文档处理完成

✅ 导出完成: 1250 个块
   保存到: final_corpus.jsonl

📊 切分统计:
   总块数: 1250

   按类型统计:
   - course_intro: 200 块，平均 450 字符      (不切分)
   - module_plan: 8 块，平均 800 字符         (按模块切)
   - lecture: 750 块，平均 1480 字符          (标准切分)
   - exercise: 180 块，平均 600 字符          (按题切分)
   - script: 90 块，平均 1500 字符            (标准切分)
   - references: 15 块，平均 900 字符         (不切分或按条目)
   - project: 7 块，平均 1200 字符            (按章节切)
```

---

## 🔨 第5步：构建索引

```bash
python3 -m flashrag.retriever.index_builder \
    --retrieval_method e5 \
    --model_path intfloat/multilingual-e5-base \
    --corpus_path final_corpus.jsonl \
    --save_dir ../../indexes/tum_complete_kb \
    --batch_size 64
```

---

## 🧪 第6步：测试

```python
from flashrag.retriever import Retriever

retriever = Retriever(
    method="e5",
    index_path="../../indexes/tum_complete_kb",
    model_path="intfloat/multilingual-e5-base"
)

# 测试不同类型的查询
queries = [
    # 查询课程介绍
    "What are the prerequisites for IN2064?",

    # 查询模块规划
    "How many ECTS do I need for Kernmodule?",

    # 查询讲义内容
    "Explain the BFS algorithm",

    # 查询练习题
    "Show me Exercise 1 for AI course",

    # 查询项目要求
    "What are the requirements for bachelor thesis?"
]

for query in queries:
    print(f"\n查询: {query}")
    results = retriever.search(query, top_k=2)

    for i, result in enumerate(results, 1):
        print(f"  结果{i}: {result['contents'][:150]}...")
```

---

## 📊 预期结果统计

根据你的数据量估算：

| 文档类型 | 数量 | 切分策略 | 预计块数 |
|---------|------|---------|---------|
| 课程介绍 | 200 | 不切分 | **200块** |
| 模块规划 | 8 | 按模块 | **8-16块** |
| 课程讲义 | 30 | 1500字符切分 | **600-900块** |
| 练习题 | 30 | 按题切分 | **150-300块** |
| Script | 30 | 1500字符切分 | **60-150块** |
| 参考文献 | 30 | 不切分 | **30块** |
| 项目文档 | 2 | 按章节 | **5-10块** |
| **总计** | - | - | **~1000-1600块** |

---

## ⚙️ 不同切分策略的原因

### 1. 课程介绍（200门）- **不切分** ❌

**原因**：
- 每个介绍很短（<1000字符）
- 信息紧密相关（课程代码、学分、先修课程）
- 切分会破坏完整性

**示例**：
```
IN2064 - Introduction to AI
ECTS: 6
Lecturer: Prof. Müller
Prerequisites: IN0001, MA0001
Description: This course...
```
→ 一个块就够了

---

### 2. 模块规划（8个）- **按模块切分** ✂️

**原因**：
- 每个模块相对独立
- 学生可能查询"Kernmodule需要多少学分"
- 模块之间信息不相关

**示例**：
```
Modul 1: Kernmodule (60 ECTS)
- IN2064 (6 ECTS)
- IN2118 (6 ECTS)
...

Modul 2: Vertiefungsmodule (30 ECTS)
...
```
→ 每个模块一个块

---

### 3. 课程讲义（30门）- **标准切分（1500字符）** ✅

**原因**：
- 讲义很长（几万字符）
- 包含多个章节和主题
- 必须切分才能有效检索

**示例**：
```
讲义：10000字符
→ 块1: Kapitel 1 (1500字符)
→ 块2: Kapitel 2 (1500字符)
→ 块3: Kapitel 3 (1500字符)
...
```

---

### 4. 练习题（30门）- **按题切分** 📝

**原因**：
- 每道题是独立的
- 学生可能查询"Aufgabe 3怎么做"
- 按题切分最自然

**示例**：
```
Aufgabe 1: Implementieren Sie BFS... (500字符)
→ 块1

Aufgabe 2: Zeigen Sie... (800字符)
→ 块2

Aufgabe 3: ... (1200字符)
→ 块3
```

---

### 5. Script（30门）- **标准切分（1500字符）** ✅

**原因**：
- Script类似讲义，是长文档
- 结构化内容
- 需要标准切分

---

### 6. 参考文献（30门）- **不切分** ❌

**原因**：
- 通常不长（<2000字符）
- 是列表形式
- 整体信息更有用

**示例**：
```
Literatur:
[1] Russell, S. (2020). AI...
[2] Mitchell, T. (1997). ML...
...
```
→ 一个块

---

### 7. 项目文档（2个）- **按章节切分** 📑

**原因**：
- 有明确的章节结构
- 学生可能查询"项目评分标准是什么"
- 按章节切分便于检索

**示例**：
```
1. Projektziele
2. Anforderungen
3. Bewertungskriterien
```
→ 每章一个块

---

## 🎯 总结：你的切分策略表

| 文档类型 | 切分方式 | 块大小 | 原因 |
|---------|---------|-------|------|
| 课程介绍 | ❌ 不切分 | 整个文档 | 太短，切分无意义 |
| 模块规划 | ✂️ 按模块 | 1个模块 | 模块独立 |
| 课程讲义 | ✅ 标准切分 | 1500字符 | 长文档必须切 |
| 练习题 | 📝 按题切分 | 1道题 | 题目独立 |
| Script | ✅ 标准切分 | 1500字符 | 类似讲义 |
| 参考文献 | ❌ 不切分 | 整个文档 | 列表，不长 |
| 项目文档 | 📑 按章节 | 1个章节 | 章节独立 |

---

## ✅ 快速开始命令

```bash
# 1. 组织文件到7个文件夹
mkdir -p /home/user/tum_data/{1_course_intros,2_module_plans,3_lectures,4_exercises,5_scripts,6_references,7_project_docs}

# 2. 运行提取+清洗+智能切分
cd /home/user/R3-RAG/data/scripts
bash quick_process_tum_data.sh /home/user/tum_data

# 3. 构建索引
python3 -m flashrag.retriever.index_builder \
    --corpus_path final_corpus.jsonl \
    --save_dir ../../indexes/tum_complete_kb

# 4. 完成！
```

---

**有任何问题随时问我！** 💬
