# 多语种语料库建立方案 (20门英语 + 10门德语)

## 📊 方案对比

### 方案1: 统一翻译成英语 ❌ **不推荐**

**做法:**
- 把10门德语课程翻译成英语
- 建立纯英语语料库

**优点:**
- ✅ 实现简单
- ✅ 只需英语Embedding模型
- ✅ 检索逻辑简单

**缺点:**
- ❌ 翻译会丢失信息（专业术语、细节）
- ❌ 翻译成本高（10门课×文档数量）
- ❌ 德语学生用户体验差（问德语问题得英语答案）
- ❌ 课程代码、教授名字翻译会出错

**结论:** 除非课程材料本身有官方英语版本，否则不推荐

---

### 方案2: 分别建立两个语料库 ⚠️ **可行但复杂**

**做法:**
- 语料库1: 20门英语课程 (English Corpus)
- 语料库2: 10门德语课程 (German Corpus)
- 检索时先判断问题语言，再选择对应语料库

**优点:**
- ✅ 语言隔离，不会混淆
- ✅ 可以为每种语言优化Embedding模型

**缺点:**
- ❌ 需要语言检测（问题是英语还是德语？）
- ❌ 跨语言问题无法处理（用英语问德语课程）
- ❌ 维护成本高（两套检索系统）
- ❌ 课程对比困难（英语课 vs 德语课）

**实现示例:**
```python
# 语言检测
from langdetect import detect

def route_to_corpus(question):
    lang = detect(question)
    if lang == 'en':
        return english_retriever.search(question)
    elif lang == 'de':
        return german_retriever.search(question)
    else:
        # 怎么办？
        return None
```

**结论:** 如果用户群体明确分为英语用户和德语用户，可以考虑

---

### 方案3: 多语言Embedding模型 ✅ **推荐方案**

**做法:**
- 使用多语言Embedding模型（如multilingual-e5）
- 建立**统一语料库**（英语和德语文档一起存储）
- 检索时自动匹配语义，不限语言

**优点:**
- ✅ 支持跨语言检索（英语问题 → 德语文档）
- ✅ 用户可以自由切换语言
- ✅ 维护简单（一套系统）
- ✅ 课程对比方便（可以同时检索英德课程）

**缺点:**
- ⚠️ 多语言模型性能略低于单语言模型
- ⚠️ 需要选择合适的多语言模型

**推荐模型:**

| 模型 | 优点 | 缺点 |
|------|------|------|
| **intfloat/multilingual-e5-large** | 性能好，支持100+语言 | 模型大(2.24GB) |
| **sentence-transformers/paraphrase-multilingual-mpnet-base-v2** | 平衡性能，支持50+语言 | 中等(1.11GB) |
| **intfloat/multilingual-e5-base** | 较小，性能尚可 | 性能略低 |

**实现示例:**
```python
from sentence_transformers import SentenceTransformer
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, PointStruct

# 1. 加载多语言Embedding模型
model = SentenceTransformer('intfloat/multilingual-e5-large')

# 2. 处理所有课程文档（英语+德语）
documents = [
    # 英语课程
    {"id": 1, "course": "IN2064", "text": "Machine Learning focuses on...", "lang": "en"},
    {"id": 2, "course": "IN2062", "text": "Deep Learning covers neural networks...", "lang": "en"},
    # 德语课程
    {"id": 3, "course": "MA1001", "text": "Lineare Algebra behandelt Vektorräume...", "lang": "de"},
    {"id": 4, "course": "PH0001", "text": "Experimentalphysik I umfasst Mechanik...", "lang": "de"},
]

# 3. 生成Embeddings（多语言模型会自动处理）
embeddings = model.encode([doc["text"] for doc in documents])

# 4. 存入向量数据库
client = QdrantClient(":memory:")
client.create_collection(
    collection_name="tum_courses",
    vectors_config=VectorParams(size=1024, distance=Distance.COSINE)
)

points = [
    PointStruct(
        id=doc["id"],
        vector=embeddings[i].tolist(),
        payload={
            "course": doc["course"],
            "text": doc["text"],
            "lang": doc["lang"]
        }
    )
    for i, doc in enumerate(documents)
]
client.upsert(collection_name="tum_courses", points=points)

# 5. 检索（支持跨语言）
def search(question, top_k=5):
    query_vector = model.encode(question)
    results = client.search(
        collection_name="tum_courses",
        query_vector=query_vector,
        limit=top_k
    )
    return results

# 测试
print("英语问题 → 英语文档:")
search("What is covered in Machine Learning?")

print("德语问题 → 德语文档:")
search("Was behandelt Lineare Algebra?")

print("英语问题 → 德语文档 (跨语言):")
search("What topics are covered in Linear Algebra?")
# → 可能会检索到德语课程 MA1001!
```

**结论:** **最推荐！** 灵活、强大、易维护

---

### 方案4: 双语并存 + 语言过滤 ⚡ **最佳实践**

**做法:**
- 使用方案3的多语言Embedding模型
- 但在检索时**允许用户指定语言偏好**
- 默认跨语言检索，需要时过滤

**优点:**
- ✅ 继承方案3的所有优点
- ✅ 可以根据需求灵活控制
- ✅ 用户体验最好

**实现示例:**
```python
def search_with_language_preference(question, top_k=5, preferred_lang=None):
    """
    检索文档，支持语言偏好

    Args:
        question: 问题
        top_k: 返回Top-K结果
        preferred_lang: 'en', 'de', 或 None (不限)
    """
    query_vector = model.encode(question)

    # 如果指定语言，添加过滤条件
    filter_condition = None
    if preferred_lang:
        filter_condition = {
            "must": [
                {"key": "lang", "match": {"value": preferred_lang}}
            ]
        }

    results = client.search(
        collection_name="tum_courses",
        query_vector=query_vector,
        query_filter=filter_condition,
        limit=top_k
    )
    return results

# 使用示例
# 1. 不限语言（跨语言检索）
search_with_language_preference("Prerequisites for Machine Learning?")

# 2. 只要英语文档
search_with_language_preference("Prerequisites for Machine Learning?", preferred_lang="en")

# 3. 只要德语文档
search_with_language_preference("Voraussetzungen für Maschinelles Lernen?", preferred_lang="de")
```

**进阶:自动语言检测 + 偏好**
```python
from langdetect import detect

def smart_search(question, top_k=5, auto_prefer_same_lang=True):
    """
    智能检索:自动检测问题语言,优先返回同语言文档
    """
    # 检测问题语言
    question_lang = detect(question)

    if auto_prefer_same_lang:
        # 先检索同语言文档
        same_lang_results = search_with_language_preference(
            question,
            top_k=top_k,
            preferred_lang=question_lang
        )

        # 如果结果不够，补充其他语言文档
        if len(same_lang_results) < top_k:
            other_results = search_with_language_preference(
                question,
                top_k=top_k - len(same_lang_results),
                preferred_lang=None  # 不限语言
            )
            return same_lang_results + other_results
        return same_lang_results
    else:
        # 直接跨语言检索
        return search_with_language_preference(question, top_k=top_k)

# 测试
# 英语问题 → 优先英语文档，但也会返回相关德语文档
smart_search("What are the prerequisites for Linear Algebra courses?")

# 德语问题 → 优先德语文档
smart_search("Was sind die Voraussetzungen für Lineare Algebra?")
```

**结论:** **工程最佳实践！** 既支持跨语言，又能满足特定需求

---

## 🎯 推荐实施路线

### 阶段1: MVP (最小可行产品)

**选择:** 方案3 (多语言Embedding模型)

**步骤:**
1. 使用 `intfloat/multilingual-e5-large`
2. 建立统一语料库（20英+10德）
3. 简单检索（不做语言过滤）

**代码量:** 少 (~200行)
**开发时间:** 1-2天

---

### 阶段2: 优化版本

**选择:** 方案4 (双语并存 + 语言过滤)

**新增功能:**
1. 语言检测
2. 语言偏好过滤
3. 智能混合检索

**代码量:** 中 (~500行)
**开发时间:** 3-5天

---

## 📋 具体实施细节

### 1. 文档预处理

```python
# 为每个课程文档添加元数据
def process_course_materials(course_folder):
    """
    处理课程材料

    course_folder/
      EN2064_ML/
        syllabus.pdf
        lecture_1.pdf
      MA1001_LinAlg/
        skript.pdf (德语)
    """
    documents = []

    for course_dir in os.listdir(course_folder):
        # 检测课程语言（基于课程代码或文件内容）
        lang = detect_course_language(course_dir)

        for doc in os.listdir(os.path.join(course_folder, course_dir)):
            text = extract_text(doc)

            # 分块 (Chunking)
            chunks = split_into_chunks(text, chunk_size=512, overlap=50)

            for i, chunk in enumerate(chunks):
                documents.append({
                    "id": f"{course_dir}_chunk_{i}",
                    "course": course_dir,
                    "text": chunk,
                    "lang": lang,
                    "source": doc
                })

    return documents
```

### 2. Embedding生成

```python
from sentence_transformers import SentenceTransformer

# 加载模型
model = SentenceTransformer('intfloat/multilingual-e5-large')

# 批量生成Embeddings
batch_size = 32
all_embeddings = []

for i in range(0, len(documents), batch_size):
    batch = documents[i:i+batch_size]
    texts = [doc["text"] for doc in batch]
    embeddings = model.encode(texts, show_progress_bar=True)
    all_embeddings.extend(embeddings)
```

### 3. 向量数据库选择

| 数据库 | 优点 | 缺点 | 适用场景 |
|--------|------|------|----------|
| **FAISS** | 极快，支持大规模 | 无元数据过滤，内存消耗大 | 纯向量检索 |
| **Qdrant** | 支持过滤，易用 | 相对较慢 | 需要元数据过滤(推荐) |
| **Milvus** | 企业级，功能强 | 部署复杂 | 大规模生产环境 |

**推荐:** Qdrant (支持语言过滤)

### 4. 多语言提示词

即使用多语言检索，生成答案时也要处理多语言:

```python
SYSTEM_PROMPT = """
You are a TUM course advisor assistant.

IMPORTANT:
- User questions may be in English or German
- Retrieved documents may be in English or German
- ALWAYS respond in the SAME language as the user's question
- If user asks in English → answer in English
- If user asks in German → answer in German (Auf Deutsch antworten)

Retrieved Documents:
{documents}

Question: {question}

Answer (in the same language as the question):
"""
```

---

## 🔍 测试验证

### 测试用例

```python
test_cases = [
    # 英语问题 → 英语文档
    {
        "question": "What are the prerequisites for Machine Learning?",
        "expected_lang": "en",
        "expected_course": "IN2064"
    },
    # 德语问题 → 德语文档
    {
        "question": "Was sind die Voraussetzungen für Lineare Algebra?",
        "expected_lang": "de",
        "expected_course": "MA1001"
    },
    # 跨语言：英语问题 → 德语文档
    {
        "question": "What topics are covered in Experimentalphysik?",
        "expected_lang": "de",  # 文档是德语
        "expected_course": "PH0001"
    },
    # 跨语言：德语问题 → 英语文档
    {
        "question": "Was lernt man in Deep Learning?",
        "expected_lang": "en",  # 文档是英语
        "expected_course": "IN2062"
    }
]

# 运行测试
for test in test_cases:
    results = search(test["question"], top_k=3)
    print(f"Question: {test['question']}")
    print(f"Top result: {results[0].payload['course']} ({results[0].payload['lang']})")
    print(f"Expected: {test['expected_course']} ({test['expected_lang']})")
    print("---")
```

---

## 💡 最终推荐

**选择方案4 (双语并存 + 语言过滤)**

**理由:**
1. ✅ 支持跨语言检索（英语学生也能找到德语课程）
2. ✅ 可以按需过滤语言（德语学生优先看德语材料）
3. ✅ 一套系统维护（不需要两个检索器）
4. ✅ 用户体验好（自动适配问题语言）
5. ✅ 扩展性强（未来加中文课程也容易）

**核心技术栈:**
- Embedding: `intfloat/multilingual-e5-large`
- 向量数据库: Qdrant
- 语言检测: langdetect
- 生成模型: DeepSeek/GPT-4 (本身支持多语言)

---

## 📦 完整代码示例

见下一个文件: `multilingual_retriever.py`
