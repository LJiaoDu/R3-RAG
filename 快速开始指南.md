# 多语言RAG系统快速开始指南

## 🎯 你的情况
- 30门课程：20门英语 + 10门德语
- 目标：建立TUM课程咨询系统

## ⚡ 5分钟快速开始

### 步骤1: 安装依赖
```bash
pip install sentence-transformers qdrant-client langdetect openai
```

### 步骤2: 准备课程材料

创建文件夹结构:
```
courses/
  IN2064_MachineLearning/
    syllabus.txt
    lecture_notes.txt
  IN2062_DeepLearning/
    course_info.txt
  MA1001_LineareAlgebra/
    skript.txt
  MA2009_Wahrscheinlichkeitstheorie/
    vorlesung.txt
  ...
```

### 步骤3: 运行完整系统

```bash
# 方法1: 运行完整演示
python 端到端多语言RAG系统.py

# 方法2: 分步执行
python prepare_corpus.py          # 准备语料库
python multilingual_retriever.py  # 测试检索器
```

---

## 📊 核心技术栈

| 组件 | 技术选择 | 原因 |
|------|---------|------|
| **Embedding** | multilingual-e5-large | 最强多语言模型 |
| **向量数据库** | Qdrant | 支持元数据过滤 |
| **语言检测** | langdetect | 轻量快速 |
| **LLM** | DeepSeek-Chat | 支持多语言，性价比高 |

---

## 🔧 配置选项

### 选项1: 轻量级配置 (快速原型)
```python
retriever = MultilingualRetriever(
    embedding_model="intfloat/multilingual-e5-base",  # 768维, 1.11GB
    collection_name="tum_courses_lite",
    qdrant_path=":memory:"  # 内存模式，不持久化
)
```

**适用场景**: 快速测试、小规模数据(<1000文档)

### 选项2: 生产级配置 (推荐)
```python
retriever = MultilingualRetriever(
    embedding_model="intfloat/multilingual-e5-large",  # 1024维, 2.24GB
    collection_name="tum_courses_prod",
    qdrant_path="./qdrant_storage"  # 持久化存储
)
```

**适用场景**: 生产环境、大规模数据(>1000文档)

---

## 💡 常见场景

### 场景1: 英语学生问英语课程
```python
question = "What are the prerequisites for Machine Learning?"

# 检索: 优先返回英语文档
results = retriever.smart_search(question, top_k=5)

# 生成: 英语答案
# → "Machine Learning (IN2064) requires Linear Algebra (MA1001) and Probability Theory (MA2009)."
```

### 场景2: 德语学生问德语课程
```python
question = "Was sind die Voraussetzungen für Lineare Algebra?"

# 检索: 优先返回德语文档
results = retriever.smart_search(question, top_k=5)

# 生成: 德语答案
# → "Lineare Algebra (MA1001) hat keine Voraussetzungen. Es ist ein Grundkurs."
```

### 场景3: 英语学生问德语课程 (跨语言)
```python
question = "What topics are covered in Wahrscheinlichkeitstheorie?"

# 检索: 找到德语文档 MA2009
results = retriever.search(question, top_k=5)

# 生成: 英语答案(基于德语文档内容)
# → "Wahrscheinlichkeitstheorie (MA2009) covers random variables, distributions, expectation value, and limit theorems."
```

### 场景4: 对比不同语言的课程
```python
question = "Compare Machine Learning and Maschinelles Lernen courses"

# 检索: 同时返回英语和德语课程
results = retriever.search(question, top_k=10, preferred_lang=None)

# 生成: 对比答案
```

---

## 🎓 进阶功能

### 功能1: 按学期过滤
```python
# 添加文档时包含学期信息
documents = [
    {
        "id": "IN2064_1",
        "text": "Machine Learning course...",
        "course": "IN2064",
        "lang": "en",
        "metadata": {"semester": "WS"}  # 冬季学期
    }
]

# 检索时过滤
from qdrant_client.models import Filter, FieldCondition, MatchValue

filter_ws = Filter(
    must=[
        FieldCondition(key="metadata.semester", match=MatchValue(value="WS"))
    ]
)

results = client.search(
    collection_name="tum_courses",
    query_vector=query_vector,
    query_filter=filter_ws,
    limit=5
)
```

### 功能2: 混合检索 (关键词+语义)
```python
# 使用Qdrant的混合检索
from qdrant_client.models import SearchParams

# 语义检索
semantic_results = retriever.search("Machine Learning prerequisites", top_k=5)

# 关键词检索 (通过元数据)
keyword_filter = Filter(
    must=[
        FieldCondition(key="course", match=MatchValue(value="IN2064"))
    ]
)

# 合并结果
combined_results = merge_and_rerank(semantic_results, keyword_results)
```

### 功能3: 多模态检索 (文本+图片)
```python
# 如果课程材料包含图片
# 使用 CLIP 等多模态模型

from transformers import CLIPModel, CLIPProcessor

model = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")
processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")

# 为图片生成Embedding
image = Image.open("lecture_diagram.png")
image_embedding = model.get_image_features(processor(images=image, return_tensors="pt"))

# 存入向量数据库
# 检索时可以同时匹配文本和图片
```

---

## 🚨 常见问题

### Q1: 检索到的文档语言不对怎么办？
**A:** 使用 `preferred_lang` 参数强制过滤：
```python
results = retriever.search(question, preferred_lang="en")  # 只要英语
```

### Q2: 跨语言检索效果不好？
**A:** 可能是模型问题，尝试更强的多语言模型：
```python
# 从 multilingual-e5-base 升级到 multilingual-e5-large
retriever = MultilingualRetriever(
    embedding_model="intfloat/multilingual-e5-large"
)
```

### Q3: 生成的答案语言混乱？
**A:** 强化Prompt中的语言指令：
```python
if question_lang == 'de':
    prompt = f"""WICHTIG: Du MUSST auf Deutsch antworten!

Frage: {question}
Antwort (NUR auf Deutsch):"""
```

### Q4: 向量数据库太大了？
**A:** 优化方案：
```python
# 方案1: 使用更小的Embedding模型
embedding_model = "sentence-transformers/paraphrase-multilingual-mpnet-base-v2"  # 420MB

# 方案2: 使用量化
from qdrant_client.models import VectorParams, Distance
vectors_config = VectorParams(
    size=768,
    distance=Distance.COSINE,
    quantization_config=ScalarQuantization(type="int8")  # 减少4倍存储
)

# 方案3: 增大chunk_size减少文档数
documents = process_course_folder("./courses", chunk_size=1024)  # 从512→1024
```

---

## 📈 性能优化

### 优化1: 批量插入
```python
# 不要一次一个插入
for doc in documents:
    retriever.add_documents([doc])  # ❌ 慢

# 应该批量插入
retriever.add_documents(documents)  # ✅ 快
```

### 优化2: 异步检索
```python
import asyncio

async def async_search(questions):
    tasks = [retriever.search(q) for q in questions]
    results = await asyncio.gather(*tasks)
    return results

# 同时处理多个问题
questions = ["Q1", "Q2", "Q3"]
results = asyncio.run(async_search(questions))
```

### 优化3: 缓存Embeddings
```python
import pickle

# 保存Embeddings
embeddings = model.encode(texts)
with open("embeddings_cache.pkl", "wb") as f:
    pickle.dump(embeddings, f)

# 加载Embeddings
with open("embeddings_cache.pkl", "rb") as f:
    embeddings = pickle.load(f)
```

---

## 🎯 实施建议

### 第1周: MVP
- [ ] 安装依赖
- [ ] 准备5-10门课程的材料
- [ ] 运行示例代码
- [ ] 测试基本检索功能

### 第2周: 完善
- [ ] 添加全部30门课程
- [ ] 优化chunk_size和overlap
- [ ] 调整检索Top-K
- [ ] 测试跨语言检索

### 第3周: 集成
- [ ] 集成LLM生成答案
- [ ] 优化Prompt模板
- [ ] 添加语言检测
- [ ] 部署Gradio UI

### 第4周: 优化
- [ ] 收集用户反馈
- [ ] 调整参数
- [ ] 添加日志监控
- [ ] 性能优化

---

## 📞 需要帮助？

遇到问题查看:
1. `多语种语料库方案.md` - 详细方案对比
2. `multilingual_retriever.py` - 检索器实现
3. `端到端多语言RAG系统.py` - 完整示例
4. GitHub Issues - 报告问题

---

**祝你的TUM课程咨询系统开发顺利！** 🚀
