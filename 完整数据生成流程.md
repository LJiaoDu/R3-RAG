# R3-RAG 完整数据生成流程

## 概览

```
15000个问题
    ↓
[Step 1] 生成Reference Answer (7000个，用于GRPO)
    ↓
[Step 2] 生成推理链 (2000个问题 × 6条链 = 约8000条，用于SFT)
    ↓
[Step 3] 使用Reference Answer验证推理链质量
    ↓
最终数据集：8000条高质量推理链 + 5000条Reference Answer
```

---

## Step 1: 生成Reference Answer

### 目的

为7000个问题生成**Reference Answer**，作为：
1. GRPO训练数据（5000条直接使用）
2. 推理链验证的Golden Answer（2000条）

### 工具

使用 `generate_reference_with_retrieval.py`

### 完整代码示例

```python
from generate_reference_with_retrieval import (
    generate_reference_answer,
    quality_check,
    batch_generate_with_retriever
)

# 1. 准备问题和检索器
questions = load_your_7000_questions()  # 你的问题列表
retriever = YourRetriever()  # 你的检索器

# 2. 为每个问题准备检索文档
questions_with_docs = []
for question in questions:
    docs = retriever.search(question, top_k=5)  # 检索top-5文档
    questions_with_docs.append({
        'question': question,
        'retrieved_docs': docs
    })

# 3. 批量生成Reference Answers
results = batch_generate_with_retriever(
    questions_with_docs=questions_with_docs,
    output_file="reference_answers.jsonl",
    model="gpt-4",              # 推荐gpt-4或gpt-4-turbo
    temperature=0.0,            # 必须0.0，确保稳定
    use_strict_prompt=True,     # 推荐严格版prompt
    max_tokens=800
)

# 4. 筛选高质量答案
high_quality_answers = [
    r for r in results
    if r.get('quality_score', 0) >= 0.8
]

print(f"✅ 生成了 {len(high_quality_answers)} 个高质量Reference Answers")
print(f"质量优秀率: {len(high_quality_answers)/len(results):.1%}")
```

### 输出格式

```json
{
  "question": "What are the prerequisites for Machine Learning?",
  "reference_answer": "The prerequisites for Machine Learning (IN2064) are Linear Algebra (MA1001) and Probability Theory (MA2009). You must take them in the following order: ...",
  "retrieved_docs": [...],
  "model": "gpt-4",
  "temperature": 0.0,
  "tokens_used": 1234,
  "quality_score": 0.95,
  "quality_checks": {...}
}
```

### 关键参数

| 参数 | 推荐值 | 原因 |
|------|--------|------|
| model | gpt-4 或 gpt-4-turbo | 质量稳定 |
| temperature | 0.0 | 确保一致性 |
| use_strict_prompt | True | 高质量要求 |
| retriever_top_k | 5 | 足够多跳推理 |

### Prompt核心设计

```
System:
- 角色: TUM课程咨询专家
- 用途: 生成Gold Standard
- 5个质量维度: ACCURACY, SPECIFICITY, COMPLETENESS, CLARITY, VERIFIABILITY

User:
- 学生问题
- 检索到的文档（结构化格式）
- 详细指导（多跳推理、包含课程代码、ECTS等）
```

详见: `Reference_Answer_Prompt设计详解.md`

### 质量控制

1. **自动检查**（7个维度）
   - 长度合理
   - 包含课程代码
   - 包含ECTS
   - 基于文档
   - 无模糊表述
   - 包含结构化信息
   - 答案完整

2. **质量标准**
   - ≥80%: 优秀，可作为Golden Answer
   - 60-80%: 一般，建议重新生成
   - <60%: 不合格，必须重新生成

3. **人工抽查**
   - 随机抽取10-20个样本
   - 确认质量符合预期

### 成本估算

```
7000个问题 × 1500 tokens/问题 = 10,500,000 tokens

gpt-4-turbo:
- 输入: $2.50/1M tokens
- 输出: $10.00/1M tokens
- 估算成本: ~$140 USD

时间: 约2-4小时（异步批处理）
```

---

## Step 2: 生成推理链

### 目的

为2000个问题生成推理链，每个问题生成6条链（使用投票/锚点过滤后保留约3-4条）

### 策略选择

你有三种策略：

| 策略 | 保留链数 | 优点 | 适用场景 |
|------|---------|------|---------|
| 纯投票 | 3-4条 | 容错性强 | 追求多样性 |
| 纯锚点 | 2-3条 | 稳定可靠 | 追求保守 |
| **混合策略** | 3条 | **质量最高** | **推荐** |

**推荐**: 混合策略（见`anchor_based_filtering.py`）

### 生成参数

```python
CONFIG = {
    "num_attempts_per_question": 6,
    "temperature_anchor": 0.0,        # 第1条（锚点）
    "temperature_exploratory": 0.9,   # 第2-6条（探索）
    "max_search_steps": 7,            # 每条链最多7步
    "top_k_docs_per_step": 3,         # 每步检索top-3
}
```

### 完整代码示例

```python
from anchor_based_filtering import hybrid_filtering

# 1. 选择2000个问题（这些问题要有reference answer）
sft_questions = select_2000_questions_with_reference_answers()

# 2. 为每个问题生成6条链
all_chains = []

for question_data in sft_questions:
    question = question_data['question']
    reference_answer = question_data['reference_answer']

    print(f"\n生成推理链: {question}")

    # 2.1 生成锚点链 (temp=0.0)
    anchor_chain = generate_reasoning_chain(
        question=question,
        temperature=0.0,
        max_steps=7,
        top_k=3
    )

    # 2.2 生成探索性链 (temp=0.9)
    exploratory_chains = []
    for i in range(5):
        chain = generate_reasoning_chain(
            question=question,
            temperature=0.9,
            max_steps=7,
            top_k=3
        )
        exploratory_chains.append(chain)

    all_attempts = [anchor_chain] + exploratory_chains

    # 2.3 混合过滤（锚点+投票）
    valid_chains, outliers = hybrid_filtering(
        chains=all_attempts,
        anchor=anchor_chain,
        anchor_threshold=0.6,
        deviation_threshold=0.5
    )

    print(f"  生成: 6条")
    print(f"  保留: {len(valid_chains)}条")
    print(f"  过滤: {len(outliers)}条")

    # 2.4 使用Reference Answer验证
    verified_chains = []
    for chain in valid_chains:
        # 计算与reference answer的F1 score
        f1 = compute_f1_score(chain['answer'], reference_answer)
        if f1 >= 0.7:  # 阈值0.7
            chain['verified'] = True
            chain['f1_score'] = f1
            verified_chains.append(chain)
        else:
            print(f"  ⚠️  F1={f1:.2f} < 0.7，过滤")

    all_chains.extend(verified_chains)

    print(f"  最终保留: {len(verified_chains)}条 (验证通过)")

print(f"\n✅ 总共生成 {len(all_chains)} 条高质量推理链")
```

### 推理链格式

```json
{
  "question": "What are the prerequisites for ML?",
  "reasoning_steps": [
    {
      "step": 1,
      "thought": "I need to find information about Machine Learning course",
      "search_query": "Machine Learning IN2064 prerequisites",
      "retrieved_docs": [...],
      "observation": "Found that ML requires LA and Prob"
    },
    {
      "step": 2,
      "thought": "I should check what Probability Theory requires",
      "search_query": "Probability Theory MA2009 prerequisites",
      "retrieved_docs": [...],
      "observation": "Prob requires Linear Algebra"
    },
    {
      "step": 3,
      "thought": "Now I can construct the complete prerequisite chain",
      "action": "answer",
      "answer": "The prerequisites for Machine Learning (IN2064) are..."
    }
  ],
  "final_answer": "The prerequisites for...",
  "temperature": 0.9,
  "num_steps": 3,
  "verified": true,
  "f1_score": 0.85
}
```

### 过滤机制

#### 混合策略（推荐）

```
生成6条链
  ↓
[过滤1] 锚点过滤
  - 与temp=0的锚点计算相似度
  - 保留相似度 >= 0.6的链
  ↓
[过滤2] 投票过滤
  - 在通过锚点过滤的链中提取实体
  - 找出多数实体（>=50%）
  - 保留偏差 <= 0.5的链
  ↓
[验证] Reference Answer验证
  - 与golden answer计算F1 score
  - 保留F1 >= 0.7的链
  ↓
最终保留约3条高质量链
```

### 成本估算

```
2000个问题 × 6条链 × 平均3.8步 × (800 tokens prompt + 500 tokens answer)
= 约30M tokens

gpt-4-turbo:
- 估算成本: ~$400 USD

时间: 约6-10小时（取决于并发数）
```

---

## Step 3: 验证和质量控制

### 验证流程

```python
# 1. 使用Reference Answer验证
for chain in chains:
    answer = chain['final_answer']
    reference = chain['reference_answer']

    # F1 score
    f1 = compute_f1_score(answer, reference)

    # 实体匹配
    answer_entities = extract_course_entities(answer)
    reference_entities = extract_course_entities(reference)
    entity_match = len(answer_entities & reference_entities) / len(reference_entities)

    # 判断
    if f1 >= 0.7 or entity_match >= 0.8:
        chain['verified'] = True
    else:
        chain['verified'] = False
        print(f"⚠️  验证未通过: F1={f1:.2f}, Entity={entity_match:.2f}")
```

### 质量指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 验证通过率 | ≥80% | F1 score ≥ 0.7 |
| 平均链长度 | 3-4步 | 符合多跳推理 |
| 平均保留/问题 | 3-4条 | 足够训练 |
| 实体匹配度 | ≥0.8 | 关键信息正确 |

---

## 完整数据集划分

### 最终数据

```
SFT训练数据 (Cold-start):
- 8000条推理链
- 来源: 2000个问题 × 平均4条链
- 用途: 训练模型基本推理能力

GRPO训练数据 (RL):
- 5000条Reference Answers
- 来源: 7000个问题中选5000个
- 用途: 强化学习优化

测试数据:
- 2000个问题及其Reference Answers
- 来源: 15000中保留2000个
- 用途: 评估最终模型性能
```

### 数据格式

#### SFT数据 (dataset_info.json)

```json
{
  "sft_data": {
    "file_name": "sft_data.jsonl",
    "columns": {
      "prompt": "question",
      "query": "question",
      "response": "reasoning_steps",
      "history": "history"
    }
  }
}
```

#### GRPO数据

```json
{
  "grpo_data": {
    "file_name": "grpo_data.jsonl",
    "columns": {
      "prompt": "question",
      "response": "reference_answer"
    }
  }
}
```

---

## 完整时间线和成本

### 时间估算

```
Step 1: 生成7000个Reference Answers
  - 时间: 2-4小时 (异步批处理)

Step 2: 生成推理链 (2000问题 × 6链)
  - 时间: 6-10小时

Step 3: 验证和过滤
  - 时间: 1-2小时

总时间: 约10-16小时
```

### 成本估算

```
Step 1: Reference Answers
  - 7000问题 × 1500 tokens = 10.5M tokens
  - gpt-4-turbo: ~$140

Step 2: 推理链
  - 2000问题 × 6链 × 3.8步 × 1300 tokens = 约30M tokens
  - gpt-4-turbo: ~$400

总成本: ~$540 USD (使用gpt-4-turbo)

如果使用gpt-4: ~$1800 USD
如果使用gpt-3.5-turbo: ~$80 USD (但质量不稳定，不推荐)
```

---

## 检查清单

### 开始前

- [ ] 准备15000个问题
- [ ] 准备检索器（向量数据库/BM25等）
- [ ] 准备课程文档库
- [ ] 设置OpenAI API Key
- [ ] 测试小样本（10个问题）

### Step 1完成后

- [ ] 生成了7000个Reference Answers
- [ ] 质量检查：≥80%优秀
- [ ] 人工抽查：10-20个样本确认质量
- [ ] 保存为JSONL格式

### Step 2完成后

- [ ] 生成了约8000条推理链
- [ ] 平均每个问题3-4条链
- [ ] 验证通过率≥80%
- [ ] 链长度分布合理（2-7步）

### Step 3完成后

- [ ] 划分数据集：8000 SFT + 5000 GRPO + 2000 TEST
- [ ] 转换为LLaMA-Factory格式
- [ ] 准备dataset_info.json
- [ ] 测试加载数据

---

## 常见问题

### Q1: Reference Answer质量不够高怎么办？

**A**:
1. 检查检索质量（relevance score >= 0.7）
2. 调整prompt（使用strict版本）
3. 提高temperature=0.0确保稳定性
4. 人工修正低质量样本

### Q2: 推理链太短/太长怎么办？

**A**:
1. 调整max_search_steps参数
2. 检查问题复杂度（简单问题2-3步正常）
3. 优化prompt，引导多跳推理

### Q3: 成本太高怎么办？

**A**:
1. 使用gpt-4-turbo替代gpt-4（便宜70%）
2. 减少链数量（6→5或4）
3. 优化prompt减少tokens
4. 分批处理，先测试小样本

### Q4: 投票/锚点过滤后保留太少怎么办？

**A**:
1. 降低阈值（0.6→0.5）
2. 调整deviation_threshold
3. 检查生成的链是否质量本身不高
4. 增加生成链数量（6→8）

---

## 下一步

完成数据生成后：

1. **训练SFT模型**
   ```bash
   llamafactory-cli train \
     --stage sft \
     --model_name_or_path Qwen/Qwen2.5-7B \
     --dataset sft_data \
     --output_dir output/sft
   ```

2. **训练GRPO模型**
   ```bash
   llamafactory-cli train \
     --stage grpo \
     --model_name_or_path output/sft \
     --dataset grpo_data \
     --output_dir output/grpo
   ```

3. **评估模型**
   ```bash
   llamafactory-cli eval \
     --model_name_or_path output/grpo \
     --dataset test_data
   ```

---

## 总结

完整流程：

```
15000问题
  ↓
[Step 1] 检索 + GPT-4 (temp=0.0) → 7000个Reference Answers
  ↓
[Step 2] 2000个问题 × 6条链 (1条temp=0 + 5条temp=0.9) → 12000条链
  ↓
[Step 3] 混合过滤（锚点+投票） + Reference Answer验证 → 8000条高质量链
  ↓
最终数据: 8000 SFT + 5000 GRPO + 2000 TEST
  ↓
训练: SFT → GRPO → 部署
```

关键文件：
- `generate_reference_with_retrieval.py` - 生成Reference Answer
- `anchor_based_filtering.py` - 混合过滤策略
- `Reference_Answer_Prompt设计详解.md` - Prompt设计原理
- `prompt_示例展示.md` - 实际效果展示

你现在有了完整的数据生成方案！🚀
