# R3-RAG数据生成流程详解（参考原论文）

> 方案：8000条推理链 + 5000条GRPO + 2000条测试集

---

## 📊 你的方案（非常好✅）

```
SFT（推理链）:  8,000 (53.3%)
GRPO（训练）:   5,000 (33.3%)
TEST（测试）:   2,000 (13.3%)
总计:          15,000
```

**这个比例非常合理！** 平衡了SFT基础和GRPO优化。

---

## 🔄 完整数据生成流程

### 流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     阶段0: 准备工作                         │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
  收集TUM数据            建立检索系统          准备15k问题
  - 课程信息           - Chunking           - Reddit
  - 学习规则           - Embedding          - Moodle
  - Reddit问答         - FAISS索引          - 人工编写


┌─────────────────────────────────────────────────────────────┐
│              阶段1: 生成推理链（SFT数据，8000条）            │
└─────────────────────────────────────────────────────────────┘

对每个问题：重复7次尝试
  │
  ├─ 尝试1: temperature=0.0 (最确定)
  ├─ 尝试2: temperature=0.9 (探索多样性)
  ├─ 尝试3: temperature=0.9
  ├─ ...
  └─ 尝试7: temperature=0.9

单次尝试流程：

  1. [用户] 提问
     ↓
  2. [GPT-4] 生成: Thought + Action: Search[query]
     ↓
  3. [系统] 真实检索（FAISS）→ 返回top-3文档
     ↓
  4. [GPT-4] 看到检索结果 → 继续推理
     ↓
  5. 重复2-4（最多7步）
     ↓
  6. [GPT-4] Action: Finish[answer]
     ↓
  7. [系统] 验证答案正确性 → 保存成功的


┌─────────────────────────────────────────────────────────────┐
│         阶段2: 生成参考答案（GRPO+TEST，7000条）            │
└─────────────────────────────────────────────────────────────┘

对每个问题：
  │
  ├─ [GPT-4] 直接生成答案（无推理链）
  │   - 使用简化prompt
  │   - temperature=0.7
  │   - 只输出最终答案
  ↓
  保存问题+答案对


┌─────────────────────────────────────────────────────────────┐
│                  阶段3: 数据分割和质量检查                   │
└─────────────────────────────────────────────────────────────┘

  SFT数据:
    ├─ 过滤失败的尝试
    ├─ 检查格式正确性
    ├─ 人工抽查质量
    └─ 保存: sft_training_data.jsonl

  参考答案:
    ├─ 随机打乱
    ├─ 分割: 5000训练 + 2000测试
    ├─ 保存: grpo_train.jsonl, test_data.jsonl
    └─ 确保测试集无泄露
```

---

## 📝 详细步骤说明

### Step 0: 准备检索系统

#### 0.1 收集TUM课程数据

```
数据源：
├─ TUMonline API（课程信息）
├─ 课程Syllabus PDF（详细内容）
├─ 学生手册（规则、要求）
├─ Reddit r/TUM_Munich（真实问答）
└─ Moodle讨论区（问题收集）
```

#### 0.2 构建检索语料库

```python
# 1. 文档分块
chunks = []
for course in courses:
    chunks.append({
        "text": f"{course.name} ({course.code}): {course.description}",
        "metadata": {
            "course_code": course.code,
            "ects": course.ects,
            "prerequisites": course.prerequisites
        }
    })

# 2. 生成Embedding
from sentence_transformers import SentenceTransformer

model = SentenceTransformer('paraphrase-multilingual-mpnet-base-v2')
embeddings = model.encode([chunk['text'] for chunk in chunks])

# 3. 建立FAISS索引
import faiss

dimension = embeddings.shape[1]
index = faiss.IndexFlatIP(dimension)  # 内积检索
index.add(embeddings)

# 保存
faiss.write_index(index, 'tum_courses.index')
```

**输出**：检索系统（query → top-k文档）

---

### Step 1: 生成推理链（SFT数据）

#### 原论文关键参数

```python
NUM_ATTEMPTS_PER_QUESTION = 7  # 每个问题尝试7次
NUM_SEARCH_PER_ATTEMPT = 7     # 每次最多7步检索
NUM_PASSAGES_PER_RETRIEVAL = 3 # 每次检索返回3个文档
TEMPERATURE_FIRST = 0.0        # 第一次用0.0
TEMPERATURE_REST = 0.9         # 后续用0.9
```

#### 为什么7次尝试？

```
尝试1 (temp=0.0): 最保守、最确定的路径
尝试2 (temp=0.9): 探索不同的推理路径
尝试3 (temp=0.9): 更多样化
...
尝试7 (temp=0.9): 最大化数据多样性

目的：
✅ 获得多样化的推理轨迹
✅ 避免模型只学会一种推理模式
✅ 提高训练数据覆盖度
```

#### 单次生成示例

**Step 1: 初始Prompt**

```
System: You are a TUM course expert with access to a course database.
Use this format:
Thought: [Your reasoning]
Action: Search[query] OR Finish[answer]

User: Question: What courses do I need before Machine Learning?
```

**Step 2: GPT-4第一次输出**

```
Thought: I need to find the official prerequisites for Machine Learning at TUM.
Action: Search[Machine Learning IN2064 prerequisites]
```

**Step 3: 系统执行真实检索**

```python
query = "Machine Learning IN2064 prerequisites"
results = retriever.search(query, top_k=3)

# 返回:
[1] Machine Learning (IN2064) requires: Linear Algebra (MA1001), Probability Theory (MA2009), and Python programming.
[2] Prerequisites must be completed with passing grade before enrollment.
[3] Machine Learning is a core module (Kernmodul) in the ML track.
```

**Step 4: 反馈给GPT-4**

```
User: Observation:
[1] Machine Learning (IN2064) requires: Linear Algebra (MA1001), Probability Theory (MA2009), and Python programming.
[2] Prerequisites must be completed with passing grade before enrollment.
[3] Machine Learning is a core module (Kernmodul) in the ML track.

Continue reasoning...
```

**Step 5: GPT-4继续推理**

```
Thought: I found the direct prerequisites. Let me check if Linear Algebra has prerequisites.
Action: Search[Linear Algebra MA1001 prerequisites]
```

**Step 6: 再次检索**

```
Observation:
[1] Linear Algebra for Informatics (MA1001) has no prerequisites.
[2] Typically taken in the first semester.
[3] Worth 8 ECTS credits.
```

**Step 7: GPT-4继续**

```
Thought: Good, Linear Algebra has no prerequisites. Now check Probability Theory.
Action: Search[Probability Theory MA2009 prerequisites]
```

**Step 8: 检索**

```
Observation:
[1] Probability Theory (MA2009) requires Linear Algebra (MA1001).
[2] Typically taken in the second semester.
[3] Worth 6 ECTS credits.
```

**Step 9: GPT-4完成**

```
Thought: I now have the complete prerequisite chain. I can provide the full answer.
Action: Finish[To take Machine Learning (IN2064) at TUM, you need to complete:

**Direct Prerequisites:**
1. Linear Algebra for Informatics (MA1001) - 8 ECTS
   - No prerequisites
   - Typically 1st semester

2. Probability Theory (MA2009) - 6 ECTS
   - Requires: Linear Algebra
   - Typically 2nd semester

3. Python Programming
   - Can be self-taught or via courses
   - Proficiency required

**Study Path:**
Semester 1: Linear Algebra + Python
Semester 2: Probability Theory
Semester 3: Machine Learning

Total preparation time: 2-3 semesters before you can take ML.]
```

#### 保存的数据格式

```json
{
  "question": "What courses do I need before Machine Learning?",
  "reasoning_chain": [
    {
      "step": 1,
      "thought": "I need to find the official prerequisites for Machine Learning at TUM.",
      "action": "Search[Machine Learning IN2064 prerequisites]",
      "observation": "[1] Machine Learning (IN2064) requires: Linear Algebra..."
    },
    {
      "step": 2,
      "thought": "I found the direct prerequisites. Let me check if Linear Algebra has prerequisites.",
      "action": "Search[Linear Algebra MA1001 prerequisites]",
      "observation": "[1] Linear Algebra for Informatics (MA1001) has no prerequisites..."
    },
    {
      "step": 3,
      "thought": "Good, Linear Algebra has no prerequisites. Now check Probability Theory.",
      "action": "Search[Probability Theory MA2009 prerequisites]",
      "observation": "[1] Probability Theory (MA2009) requires Linear Algebra..."
    },
    {
      "step": 4,
      "thought": "I now have the complete prerequisite chain.",
      "action": "Finish",
      "answer": "To take Machine Learning (IN2064) at TUM, you need..."
    }
  ],
  "answer": "To take Machine Learning (IN2064) at TUM, you need...",
  "num_steps": 4,
  "temperature": 0.0
}
```

---

### Step 2: 生成参考答案（GRPO+TEST）

#### 简化Prompt

```
System: You are a TUM course expert.
Provide a direct, accurate answer.
Do NOT show reasoning steps.

User: What courses do I need before Machine Learning?
```

#### GPT-4输出

```
To take Machine Learning (IN2064) at TUM, you need:

1. Linear Algebra (MA1001) - 8 ECTS
   - No prerequisites, 1st semester

2. Probability Theory (MA2009) - 6 ECTS
   - Requires Linear Algebra, 2nd semester

3. Python programming proficiency

Study path: Linear Algebra → Probability Theory → Machine Learning
Total: 2-3 semesters preparation.
```

#### 保存格式

```json
{
  "question": "What courses do I need before Machine Learning?",
  "reference_answer": "To take Machine Learning (IN2064) at TUM, you need..."
}
```

**关键差异**：
- ❌ 无推理步骤
- ❌ 无检索过程
- ✅ 只有最终答案

---

## 🎯 原论文的设计智慧

### 1️⃣ 为什么SFT需要推理链？

**教学作用**：
```
模型：我不知道如何分步推理 😵
SFT数据：看这个例子，一步步来...
  Step 1: Thought → Action: Search
  Step 2: 看到结果 → 继续推理
  Step 3: 再次Search
  Step 4: Finish
模型：哦！我学会了！✅
```

### 2️⃣ 为什么GRPO不需要推理链？

**探索作用**：
```
SFT训练后：
模型：我已经会推理了！

GRPO训练时：
系统：给你一个问题，自己推理
模型：[生成自己的推理链]
系统：和参考答案比较 → reward=0.85
模型：哦，这样推理更好！优化policy...

如果GRPO也给推理链：
→ 模型只会模仿，不会探索新路径
→ 失去了强化学习的意义
```

### 3️⃣ 为什么要7次尝试？

**多样性**：
```
temperature=0.0: 保守路径
  ├─ 搜索"prerequisites"
  └─ 直接回答

temperature=0.9: 探索路径
  ├─ 搜索"study path"
  ├─ 搜索"semester planning"
  ├─ 搜索"graduation requirements"
  └─ 多角度回答

最终：模型学会多种推理策略
```

### 4️⃣ 为什么要真实检索？

**真实性**：
```
模拟检索（GPT-4想象）：
  ❌ 可能编造事实
  ❌ 检索结果不真实
  ❌ 模型学会"编造"

真实检索（FAISS）：
  ✅ 从真实文档检索
  ✅ 答案有依据
  ✅ 模型学会"基于证据推理"
```

---

## 💰 成本估算

### 推理链（8000条）

```
平均每条：
  - GPT-4调用: 4次（3次Search + 1次Finish）
  - 输入tokens: 200/次 × 4 = 800
  - 输出tokens: 150/次 × 4 = 600

8000条总计：
  - 输入: 8000 × 800 = 6,400,000 tokens → $0.96
  - 输出: 8000 × 600 = 4,800,000 tokens → $2.88
  - 小计: $3.84

考虑7次尝试（成功率80%）：
  - 实际需要: 8000 / 0.8 = 10,000次尝试
  - 实际成本: $3.84 × 10000/8000 = $4.80
```

### 参考答案（7000条）

```
平均每条：
  - GPT-4调用: 1次
  - 输入tokens: 150
  - 输出tokens: 300

7000条总计：
  - 输入: 7000 × 150 = 1,050,000 tokens → $0.16
  - 输出: 7000 × 300 = 2,100,000 tokens → $1.26
  - 小计: $1.42
```

### 总成本

```
推理链: $4.80
参考答案: $1.42
总计: $6.22

时间估算:
- 推理链: 每条2分钟 × 10000 = 333小时 ≈ 14天（单线程）
- 并行10个: 14天 → 1.4天
- 参考答案: 7000 × 10秒 = 19小时

实际可行时间: 2-3天（并行处理）
```

---

## ✅ 质量检查

### 推理链质量检查

```python
def check_sft_quality(data):
    """检查SFT数据质量"""

    issues = []

    # 1. 检查格式
    if 'reasoning_chain' not in data:
        issues.append("Missing reasoning_chain")

    # 2. 检查步数
    num_steps = len(data.get('reasoning_chain', []))
    if num_steps < 2:
        issues.append(f"Too few steps: {num_steps}")
    if num_steps > 10:
        issues.append(f"Too many steps: {num_steps}")

    # 3. 检查是否有Search
    has_search = any(
        'Search[' in step.get('action', '')
        for step in data.get('reasoning_chain', [])
    )
    if not has_search:
        issues.append("No Search action found")

    # 4. 检查是否有Finish
    has_finish = any(
        step.get('action') == 'Finish'
        for step in data.get('reasoning_chain', [])
    )
    if not has_finish:
        issues.append("No Finish action")

    # 5. 检查答案长度
    answer = data.get('answer', '')
    if len(answer) < 50:
        issues.append(f"Answer too short: {len(answer)} chars")

    return len(issues) == 0, issues
```

### 人工抽查

```python
# 随机抽取10条检查
sample = random.sample(sft_data, 10)

for i, item in enumerate(sample):
    print(f"\n{'='*60}")
    print(f"样本 {i+1}/10")
    print(f"{'='*60}")
    print(f"问题: {item['question']}")
    print(f"\n推理过程:")

    for step in item['reasoning_chain']:
        print(f"\nStep {step['step']}:")
        print(f"  Thought: {step['thought'][:100]}...")
        print(f"  Action: {step['action']}")
        if step.get('observation'):
            print(f"  Observation: {step['observation'][:100]}...")

    print(f"\n最终答案: {item['answer'][:200]}...")
    print(f"\n步数: {item['num_steps']}")

    # 人工判断
    feedback = input("\n质量评分 (1-5): ")
```

---

## 🎤 面试时怎么说？

**面试官：你如何生成训练数据的？**

**你的回答：**

> "我参考R3-RAG原论文的方法，使用GPT-4作为Teacher model，**结合真实检索系统**生成数据。
>
> **SFT阶段（8000条推理链）**：
> 1. 为每个问题尝试7次，第1次temperature=0.0，后续0.9，增加多样性
> 2. GPT-4输出Search[query]时，我用FAISS在TUM课程知识库中**真实检索**
> 3. 将检索结果反馈给GPT-4，让它基于真实文档继续推理
> 4. 重复Thought→Search→Observation，最多7步
> 5. GPT-4输出Finish[answer]时保存完整轨迹
> 6. 这样生成的推理链是基于真实检索的，有证据支撑
>
> **GRPO阶段（5000条参考答案）**：
> 1. 用简化的prompt，让GPT-4直接生成答案
> 2. 不包含推理链，因为GRPO训练时模型会自己生成推理
> 3. 参考答案用于计算reward signal
>
> **测试集（2000条）**：从GRPO数据中分离，确保不参与训练
>
> 关键点：**真实检索交互**而非想象，这让模型学会基于证据推理，避免编造。"

**专业、详细、突出真实检索的重要性！** 🎯

---

## 📚 总结

### 核心要点

1. **8000 SFT：推理链 + 真实检索**
2. **5000 GRPO：参考答案（无推理链）**
3. **2000 TEST：独立测试集**
4. **7次尝试：多样性**
5. **真实检索：避免编造**

### 与简单生成的区别

| 方面 | 简单生成 | R3-RAG方法 |
|------|---------|-----------|
| 检索 | GPT-4想象 | 真实FAISS检索 |
| 交互 | 一次性生成 | 多轮对话 |
| 多样性 | 单一路径 | 7次尝试 |
| 可靠性 | 可能编造 | 基于真实文档 |
| 成本 | 低 | 较高 |
| 质量 | 一般 | 高 |

**你的方案（8k/5k/2k + 真实检索）完全符合R3-RAG标准！** ✅

---

**准备好实现了吗？** 🚀
