# 混合模式生成：10000条推理链 + 5000条直接答案

## 🎯 为什么用混合模式？

### 1️⃣ 节省22%成本

```
混合模式：$4.10
全推理链：$5.25
节省：$1.15 (22%)
```

### 2️⃣ 数据更真实

```
复杂问题 → 推理链：
"我从零基础到学ML的完整路径是什么？"
→ 需要多步推理，查询前置课程

简单问题 → 直接答案：
"Machine Learning课程多少ECTS？"
→ 直接回答"8 ECTS"即可
```

### 3️⃣ 模型效果更好

- **学会判断**：何时需要详细推理，何时直接回答
- **避免过度推理**：简单问题不啰嗦
- **保留推理能力**：复杂问题展现思考过程

这和**人类回答**一样 - 简单问题快速回答，复杂问题详细分析！

---

## 🚀 快速使用

### Step 1: 运行脚本

```bash
python generate_mixed_format_answers.py
```

### Step 2: 选择分配策略

脚本会问你：

```
选择问题分配策略:
1. 智能分类（推荐）- 根据问题复杂度自动分配
2. 随机分配 - 完全随机
3. 按顺序分配 - 前10000个推理链，后5000个直接答案

请选择 (1/2/3):
```

**推荐选 1（智能分类）**

### Step 3: 查看结果

输出文件：`questions_with_mixed_answers.jsonl`

```jsonl
{
  "id": 1,
  "question": "What's the learning path from beginner to ML?",
  "answer": "Thought: I need to find prerequisites...\nAction: Search[...]\n...",
  "answer_type": "reasoning",
  "model": "gpt-4o-mini"
}

{
  "id": 2,
  "question": "How many ECTS is Machine Learning?",
  "answer": "Machine Learning (IN2064) is worth 8 ECTS credits.",
  "answer_type": "direct",
  "model": "gpt-4o-mini"
}
```

---

## 🧠 智能分类规则

脚本会自动判断问题复杂度：

### ✅ 分配推理链的问题

**特征：**
- 包含"path", "plan", "how to"等词
- 包含"before", "after", "then"等顺序词
- 包含"difference", "compare"等对比词
- 包含"if", "can I", "should I"等条件词
- 问题较长（>80字符）

**例子：**
```
✅ "What courses should I take before Advanced DL?"
✅ "What's the difference between ML and DL courses?"
✅ "Can I take both Computer Vision and NLP in one semester?"
✅ "Welche Kurse sollte ich für den ML Track wählen?"
```

### ✅ 分配直接答案的问题

**特征：**
- 询问具体事实（ECTS, 时间, 代码）
- 是/否问题
- 定义性问题
- 问题较短（<40字符）

**例子：**
```
✅ "How many ECTS is Machine Learning?"
✅ "When is the Computer Vision exam?"
✅ "Who teaches NLP?"
✅ "What is the course code for ML?"
✅ "Wie viele ECTS brauche ich für Master?"
```

---

## 📊 输出对比

### 推理链模式输出（400-500 tokens）

```
Question: "What courses do I need before Machine Learning?"

Answer:
Thought: I need to find the prerequisites for Machine Learning course.
Action: Search[Machine Learning IN2064 prerequisites]
Observation: Machine Learning (IN2064) requires: Linear Algebra, Probability Theory, Python.

Thought: Let me check if these have prerequisites.
Action: Search[Linear Algebra prerequisites]
Observation: Linear Algebra (MA1001) has no prerequisites.

Thought: Check Probability Theory.
Action: Search[Probability Theory prerequisites]
Observation: Probability Theory requires Linear Algebra.

Thought: I have complete information now.
Action: Finish[To take Machine Learning, you need:
1. Linear Algebra (MA1001) - no prerequisites
2. Probability Theory (MA2009) - requires Linear Algebra
3. Python programming experience

The sequence is: Linear Algebra → Probability Theory → Machine Learning
This typically takes 2-3 semesters.]
```

### 直接答案模式输出（150-200 tokens）

```
Question: "How many ECTS is Machine Learning?"

Answer:
Machine Learning (IN2064) at TUM is worth 8 ECTS credits.
```

**推理链：详细、教学式、展示思考过程**
**直接答案：简洁、高效、直达重点**

---

## 💰 成本详细对比

### 混合模式（推荐）

```
10000条推理链：
  输入: 10000 × 150 tokens = 1,500,000 tokens → $0.23
  输出: 10000 × 400 tokens = 4,000,000 tokens → $2.40
  小计: $2.63

5000条直接答案：
  输入: 5000 × 80 tokens = 400,000 tokens → $0.06
  输出: 5000 × 150 tokens = 750,000 tokens → $0.45
  小计: $0.51

总成本: $3.14
```

### 全推理链模式

```
15000条推理链：
  输入: 15000 × 150 tokens = 2,250,000 tokens → $0.34
  输出: 15000 × 400 tokens = 6,000,000 tokens → $3.60

总成本: $3.94
```

### 全直接答案模式（不推荐）

```
15000条直接答案：
  输入: 15000 × 80 tokens = 1,200,000 tokens → $0.18
  输出: 15000 × 150 tokens = 2,250,000 tokens → $1.35

总成本: $1.53
```

**结论：混合模式既省钱，又保证质量！**

---

## 🎨 自定义分配比例

如果你想调整比例（比如8000推理链 + 7000直接答案）：

```python
# 编辑 generate_mixed_format_answers.py

# 修改这两行
NUM_WITH_REASONING = 8000   # 改成你想要的数量
NUM_DIRECT_ANSWER = 7000    # 改成你想要的数量
```

**推荐比例：**
- 60%-70% 推理链（复杂问题为主）
- 30%-40% 直接答案（简单问题）

---

## 📈 质量检查

### 检查分配是否合理

```python
import json
from collections import Counter

# 读取结果
with open('questions_with_mixed_answers.jsonl', 'r') as f:
    results = [json.loads(line) for line in f]

# 抽样检查推理链问题
reasoning_samples = [r for r in results if r['answer_type'] == 'reasoning'][:10]

print("🔍 推理链问题样本（应该是复杂问题）:")
for item in reasoning_samples:
    print(f"Q: {item['question']}")
    print(f"A: {item['answer'][:100]}...\n")

# 抽样检查直接答案问题
direct_samples = [r for r in results if r['answer_type'] == 'direct'][:10]

print("🔍 直接答案问题样本（应该是简单问题）:")
for item in direct_samples:
    print(f"Q: {item['question']}")
    print(f"A: {item['answer'][:100]}...\n")
```

### 统计答案长度

```python
import numpy as np

reasoning_lengths = [
    len(r['answer'])
    for r in results
    if r['answer_type'] == 'reasoning' and r.get('answer')
]

direct_lengths = [
    len(r['answer'])
    for r in results
    if r['answer_type'] == 'direct' and r.get('answer')
]

print(f"📏 推理链答案长度: {np.mean(reasoning_lengths):.0f} ± {np.std(reasoning_lengths):.0f} 字符")
print(f"📏 直接答案长度: {np.mean(direct_lengths):.0f} ± {np.std(direct_lengths):.0f} 字符")

# 应该看到：
# 推理链: 800-1200 字符
# 直接答案: 150-300 字符
```

---

## 🔄 转换为训练格式

### 保留类型标签（推荐）

这样训练时可以看到数据分布：

```python
training_data = []

for item in results:
    if not item.get('answer'):
        continue

    training_data.append({
        "instruction": item['question'],
        "input": "",
        "output": item['answer'],
        "answer_type": item['answer_type']  # 保留类型标签
    })

with open('tum_mixed_dataset.json', 'w', encoding='utf-8') as f:
    json.dump(training_data, f, ensure_ascii=False, indent=2)
```

### 或者完全混合（也可以）

```python
training_data = []

for item in results:
    if not item.get('answer'):
        continue

    training_data.append({
        "instruction": item['question'],
        "input": "",
        "output": item['answer']
        # 不保留类型标签，完全混合
    })

# 打乱顺序
import random
random.shuffle(training_data)

with open('tum_mixed_dataset.json', 'w', encoding='utf-8') as f:
    json.dump(training_data, f, ensure_ascii=False, indent=2)
```

---

## 🎯 训练配置

在 `dataset_info.json` 中添加：

```json
{
  "tum_mixed": {
    "file_name": "tum_mixed_dataset.json",
    "formatting": "alpaca",
    "columns": {
      "prompt": "instruction",
      "query": "input",
      "response": "output"
    }
  }
}
```

然后在训练配置中：

```yaml
# llama3_full_sft_tum.yaml
dataset: tum_mixed
# 其他配置...
```

**模型会自动学会：**
- 何时输出推理链（复杂问题）
- 何时直接回答（简单问题）

训练时**不需要**指定问题类型，模型会从数据中学习！

---

## 💡 进阶技巧

### 1. 按主题分配

```python
# 修改 classify_question_complexity() 函数

def classify_by_topic(question: str) -> str:
    """按主题决定是否需要推理链"""

    # 这些主题需要推理链
    reasoning_topics = [
        'prerequisite', 'path', 'plan', 'sequence',
        'comparison', 'difference', 'choice'
    ]

    # 这些主题直接答案
    direct_topics = [
        'ects', 'credit', 'time', 'when', 'who',
        'code', 'number'
    ]

    question_lower = question.lower()

    for topic in direct_topics:
        if topic in question_lower:
            return 'direct'

    for topic in reasoning_topics:
        if topic in question_lower:
            return 'reasoning'

    return 'reasoning'  # 默认用推理链（保守策略）
```

### 2. 使用LLM分类（更准确但更慢）

```python
def classify_with_llm(question: str) -> str:
    """用GPT-4判断问题复杂度"""

    prompt = f"""Classify this question as either "simple" or "complex":

Question: {question}

- Simple: Asks for a specific fact (ECTS, time, name, code, definition)
- Complex: Requires multi-step reasoning (planning, comparison, if-then logic)

Answer with one word: simple or complex"""

    response = openai.ChatCompletion.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt}],
        temperature=0.0,
        max_tokens=10
    )

    classification = response.choices[0].message.content.strip().lower()

    return 'direct' if 'simple' in classification else 'reasoning'
```

### 3. 手动标注样本（最准确）

```python
# 准备一个手动标注的文件
manual_labels = {
    1: 'reasoning',  # question id: type
    2: 'direct',
    3: 'reasoning',
    # ...
}

# 在分配时使用
for q in questions:
    q_id = q['id']
    if q_id in manual_labels:
        q['generation_mode'] = manual_labels[q_id]
    else:
        # 自动分类
        q['generation_mode'] = classify_question_complexity(q['question'])
```

---

## 🎤 面试时怎么说？

**面试官：你有15000个问题，如何生成训练数据？**

**你的回答：**

> "我采用了**混合模式生成策略**：
>
> 1. **数据分层**：
>    - 10000条复杂问题 → 生成推理链（Thought/Action/Observation格式）
>    - 5000条简单问题 → 生成直接答案
>
> 2. **智能分类**：
>    - 自动识别问题复杂度（路径规划、对比分析 → 推理链）
>    - 事实查询、定义问题 → 直接答案
>
> 3. **优势**：
>    - **成本优化**：节省22%成本（$3.14 vs $3.94）
>    - **数据真实性**：符合实际对话场景，避免过度推理
>    - **模型效果**：训练后模型学会自动判断何时需要详细推理
>
> 4. **实现**：
>    - 用GPT-4o-mini作为Teacher model
>    - 异步批处理，5-6小时完成15000条
>    - 最终混合训练，不保留类型标签
>
> 这个策略让模型既有推理能力，又不会对简单问题过度啰嗦，更符合实际使用场景。"

**简洁、专业、有数据、有思考！** 🎯

---

## ✅ 总结

| 方案 | 成本 | 质量 | 推荐度 |
|------|------|------|--------|
| 全推理链 | $3.94 | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **混合模式** | **$3.14** | **⭐⭐⭐⭐⭐** | **⭐⭐⭐⭐⭐** |
| 全直接答案 | $1.53 | ⭐⭐ | ⭐ |

**混合模式是最优选择！**

既省钱，又能让模型学会"什么时候该详细思考，什么时候该直接回答"——这才是真正智能的表现！

---

**准备好了吗？开始生成吧！** 🚀

```bash
python generate_mixed_format_answers.py
```
